# NEXT STEPS

Ordered follow-up actions to keep project memory and quality healthy.

1. Add lightweight automated tests
- Start with route smoke tests and one projects-page rendering test.
- Goal: catch regressions in route map and critical UI composition.

2. Keep memory docs in sync
- Refresh `INDEX.md` and `ARCHITECTURE.md` after structural changes (routes, modules, workflows).
- Refresh `CURRENT_STATE.md` after notable releases or refactors.

3. Clean tracked noise files
- Evaluate removal of tracked `.DS_Store` files in a dedicated cleanup change.
